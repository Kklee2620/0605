# Frontend Environment Variables
VITE_API_BASE_URL=http://localhost:3001

# For vite.config.ts if used (ensure this key is safe to expose or handled via backend)
# GEMINI_API_KEY=your_gemini_api_key_for_frontend_if_needed

# Note: Backend environment variables are in backend/.env.example
